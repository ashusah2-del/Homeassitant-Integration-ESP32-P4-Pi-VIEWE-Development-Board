[Unit]
Description=Periodically sync iHost mDNS name to panel-hub hub.env

[Timer]
# Run 1 min after boot (self-heal after a power blip), then once a day.
OnBootSec=1min
OnUnitActiveSec=1d
Persistent=true

[Install]
WantedBy=timers.target
